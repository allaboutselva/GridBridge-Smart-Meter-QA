[project]
name = "gridbridge-smart-meter-qa"
version = "2.0.0"
requires-python = ">=3.11"
dependencies = ["fastapi", "uvicorn[standard]", "mysql-connector-python", "python-dotenv", "PyJWT", "requests"]

[dependency-groups]
dev = ["pytest", "allure-pytest"]
